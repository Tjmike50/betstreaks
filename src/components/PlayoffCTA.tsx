// Playoff Pass has been retired in favor of the unified Premium pricing
// (monthly, yearly, lifetime, all-apps lifetime). This component is kept
// as a no-op for backward compatibility with existing imports.
export function PlayoffCTA() {
  return null;
}
